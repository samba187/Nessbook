{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm ci || npm i",
  "framework": null,
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
