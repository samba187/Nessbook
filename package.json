{
  "name": "nessbook-root",
  "private": true,
  "engines": { "node": "20.x" },
  "scripts": {
    "heroku-postbuild": "npm --prefix booker-app install --include=dev && npm --prefix booker-app run build"
  }
}
